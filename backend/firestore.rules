rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all — all access goes through backend API
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
